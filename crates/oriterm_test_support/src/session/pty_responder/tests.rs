//! Sibling tests for `pty_responder/mod.rs`.
//!
//! Tests exercising `PtyResponder`'s event dispatch land here. Section
//! `06.0.c` (PtyResponder OSC-event extension) fills this file with the
//! ColorRequest / ClipboardLoad / ClipboardStore round-trip pins and the
//! PtyWrite regression guard. The file exists as a stub in the Step 1
//! (proactive split) commit so the module layout is final before any
//! functional change lands.
