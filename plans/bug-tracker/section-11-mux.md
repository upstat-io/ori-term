---
section: 11
title: "Mux & Pane I/O"
domain: "oriterm_mux — pane server, PTY I/O, IO thread, pane lifecycle, backpressure"
status: in-progress
---

# Section 11: Mux & Pane I/O

Bugs in the pane multiplexer — PTY I/O, IO thread behavior, pane lifecycle, memory management.

## Open Bugs

- [ ] `[BUG-11-17][low]` **OSC 133 / OSC 633 `;D[;exitcode]` silently drops the optional exit-code parameter** — found by BUG-08-12 impl-hygiene-review F10-exit-code (codex surfacing, verified against code).
  Repro: feed `\x1b]133;D;42\x1b\\` (or `\x1b]633;D;42\x1b\\`) through `RawInterceptor::osc_dispatch`. Expected: `HostEffect::CommandComplete` (or an extension) carries the exit code `42` so downstream consumers (tab badge, prompt-status rendering, notification on failure) can distinguish success from failure. Actual: both `handle_osc133` (now `handle_prompt_action`) and `handle_osc633` `b'D'` arms call `self.term.finish_command(None)` and push `HostEffect::CommandComplete { duration }` with no exit-code field; `params.get(2)` is never read.
  Subsystem: `oriterm_mux/src/shell_integration/interceptor.rs` (shared `handle_prompt_action` after BUG-08-12 impl-hygiene F1 extraction), `oriterm_core/src/effect/` (needs `HostEffect::CommandComplete { duration, exit_code: Option<i32> }` or a new variant).
  Analysis: GAP — the interceptor doc comment at `interceptor.rs:142` explicitly mentions `D[;exitcode]`, and the VS Code shell-integration source emits the third parameter for failed commands. The `HostEffect::CommandComplete` payload would need to be extended (additive field, easy migration) and the `;D` handler updated to parse `params.get(2)` via `std::str::from_utf8` + `i32::parse` with graceful fallback on malformed input. Not a regression from BUG-08-12 — was already silently dropped before; surfaced by the cross-check pass.
  TDD matrix: (1) `;D` alone → effect emitted with `exit_code: None` (backwards-compatible), (2) `;D;0` → `exit_code: Some(0)`, (3) `;D;42` → `exit_code: Some(42)`, (4) `;D;-1` negative → `Some(-1)` (POSIX signals are positive but some integrations use negative), (5) `;D;garbage` → `None` graceful fallback, (6) mirror scenarios for OSC 633.
  Found: 2026-04-24 | Source: BUG-08-12 impl-hygiene-review F10-exit-code

- [ ] `[BUG-11-16][medium]` **Desktop notifications (OSC 9 / 99 / 777) produce no OS sound; no user setting to control behavior** — found by manual.
  Repro: In a shell that emits OSC 9 on command completion (shell integration / `notify_command_bell = true`), run a long command that triggers a prompt notification. Expected: OS toast/notification banner AND an audible sound. Actual: visual notification appears (on Linux via `notify-send`, Windows via BurntToast/WinRT toast, macOS via `osascript display notification`) but no sound is played. Also true for OSC 777 (rxvt-style) and OSC 9;4 progress notifications.
  Subsystem: `oriterm/src/platform/notify/mod.rs` (dispatch — no sound parameter passed to any platform backend), `oriterm/src/app/mux_pump/mod.rs:137-143` (`MuxNotification::DesktopNotification` handler — calls `notify::send(&title, &body)` with no behavior knob), `oriterm/src/config/` (no notification-behavior config struct — only `behavior.bell` for BEL and `behavior.notify_command_bell` boolean for gating OSC 9 emission, neither covers sound/notification/none choice for the notification itself).
  Analysis: All three platform paths currently fire "silent" notifications by default:
  - Linux: `notify-send` with no `--hint=string:sound-name:message-new-instant` / `-h "string:sound-name:..."` — freedesktop notification servers default to silent unless the spec sound hint is set or the user configures sound-on-notification system-wide.
  - Windows: BurntToast `New-BurntToastNotification` without `-Sound` parameter → silent toast; the raw `Windows.UI.Notifications` XML fallback also omits `<audio>` element → silent.
  - macOS: `osascript -e 'display notification "..." with title "..."'` without the `sound name "..."` clause → silent.

  Root fix is two-sided: (1) extend `platform::notify::send` to accept a `NotificationOptions` struct (or pair of `send` / `send_with_sound` fns) and wire each platform backend to request the OS default notification sound when requested. (2) add `behavior.notification = "none" | "visual" | "sound" | "both"` (default: `"both"`) in `oriterm/src/config/` mirroring the planned `behavior.bell` tri-state from roadmap §27, and have `mux_pump` consult the config before calling `notify::send`. The Lua `LuaCommand::SendNotification` callback (roadmap §28 extensibility) should pick up the same options so scripted notifications respect the user setting.

  Scope boundary vs existing bugs:
  - BUG-08-1 (`\a` audible bell) is distinct — that covers ECMA-48 BEL char playing a sound; this bug covers OSC 9/99/777 desktop notifications playing a sound.
  - BUG-11-9 (`HostEffect::AudioRequest` / DECPS) is distinct — that covers the DECPS play-sound control sequence for arbitrary tones, not notification sounds.
  - Roadmap §27 plans `behavior.bell = "none" | "visual" | "notification"` for BEL → this bug's fix should land a sibling `behavior.notification` tri-state consistent with that naming pattern, not reuse the bell config.
  Found: 2026-04-19 | Source: manual
  Note: Active work in roadmap §27 (command palette, plans `behavior.bell` tri-state) and §20 (shell integration, shipped `behavior.notify_command_bell` gate) touches this area but neither covers OS notification sound or the missing `behavior.notification` config. §28 (extensibility) mentions Lua `SendNotification` — its API should be updated in the same fix so Lua-triggered notifications inherit the same user setting.

- [ ] `[BUG-11-15][low]` **Cross-crate `drain_desktop_notifications` test-helper duplication** — found by tpr-review.
  Repro: `oriterm_core/src/term/tests.rs:28-30` defines `drain_desktop_notifications(&Term<QueueingEffectSink>) -> Vec<(NotificationSource, String, String)>` and `oriterm_mux/src/shell_integration/tests.rs:222` defines `drain_desktop_notifications(&Term<QueueingEffectSink>) -> Vec<TestNotification>` (named-field struct). Both helpers drain `Term<QueueingEffectSink>` effects and apply `ClearPendingNotifications` semantics (clear preceding queued notifications). Identical control-flow skeleton, divergent return-type shape, two crates.
  Subsystem: `crates/oriterm_test_support/` (the consolidation target) + the two test sites above.
  Analysis: Per `.claude/rules/impl-hygiene.md` §Algorithmic DRY → "Cross-crate duplication: even 2 instances = extract to a shared crate or shared type." Right consolidation is to introduce a shared helper in `crates/oriterm_test_support` (e.g. `notifications::drain(term: &Term<S>) -> Vec<DrainedNotification>` returning a single named-field struct) and migrate both call sites to it. Fix-time prerequisite: `oriterm_test_support` is currently NOT in `oriterm_mux/Cargo.toml [dev-dependencies]` (only `tempfile = "3"` is listed). The §10.3 plan text explicitly opted out of adding it for that subsection, so this fix is its own architectural step — adding the dev-dep, designing the shared `DrainedNotification` shape, then migrating both sites + the §10.3 OSC 9/99/777 tests onto it. Severity low because the duplication is test-only and ~10 lines per site; fix is opportunistic — bundle with the next mux change that already adds `oriterm_test_support` to its dev-deps.
  Found: 2026-04-19 | Source: tpr-review
  Note: Filed by spec-conformance §10.3 TPR checkpoint 2 round 0 — gemini finding F1 (LOWER trust, verified via cross-crate grep). The §10.3 implementation extended the mux helper (added `source` field) without touching the core helper, widening the existing shape divergence. Reviewer: gemini.

- [x] `[BUG-11-14][low]` **`oriterm_mux/src/pane/io_thread/mod.rs` is 558 lines — over the 500-line hard limit** — found by impl-hygiene-review.
  Found: 2026-04-18 | Source: impl-hygiene-review
  Fixed: 2026-04-22 — Resolved as the broken-window precondition for spec-conformance §13.3 (kitty graphics animation wiring). Extracted `run()`, `handle_pty_eof()`, and `spawn()` into a new `oriterm_mux/src/pane/io_thread/run_loop.rs` submodule (201 lines). `mod.rs` dropped to 387 lines. The two duplicated `crossbeam_channel::select!` branches (sync-deadline + no-deadline) also collapsed into a single unified-deadline branch that consumes `min(sync_deadline, animation_deadline)` with an `IDLE_WAKE_CEILING` sentinel (24h) when neither is active — the animation-tick addition would have duplicated the arms a third time, so the unification landed in the same commit. All 25 `oriterm_mux` unit tests remain green.

- [ ] `[BUG-11-13][medium]` **OSC 10/11/12 theme-color HostColorQuery returns placeholder `Rgb { r:0, g:0, b:0 }` instead of real palette color** — found by continue-roadmap.
  Repro: Run `printf '\e]10;?\e\\'` (OSC 10 = foreground color query) in oriterm — terminal replies with `rgb:0000/0000/0000` (black) regardless of the configured foreground color. Same for OSC 11 (background) and OSC 12 (cursor). Apps relying on theme detection (vim background detection, delta, bat) misidentify the theme.
  Subsystem: `oriterm/src/app/mux_pump/` — the `MuxNotification::HostColorQuery` handler fulfills the `ResponseToken<Rgb>` with a hardcoded `Rgb { r: 0, g: 0, b: 0 }` placeholder rather than querying the active `Palette` via the session/window context.
  Analysis: Core correctly emits `HostRequest::ColorQuery { index, .. }` from `oriterm_core/src/term/handler/osc.rs`. The effect→MuxEvent router emits `MuxEvent::HostColorQuery { pane_id, prefix, index, terminator, reply }` with the real `ResponseToken<Rgb>`. The mux_pump handler currently calls `mux.fulfill_host_request(pane_id, HostReply::ColorQuery { token, color: Rgb { r:0, g:0, b:0 } })` — placeholder pending palette wiring. The fix needs the handler to look up the pane's `Palette` and resolve the color by index (0–15 = ANSI, 256 = special palette indices for OSC 10/11/12). Cross-ref: BUG-11-3 was the legacy-path version; this is the new effect-cutover version of the same end-to-end gap.
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Effect-cutover §01.1+01.2 wired the round-trip plumbing (request → token → fulfill → PTY reply); palette resolution was deferred per blind-spot §8 to keep §01.1 atomic.

- [ ] `[BUG-11-12][low]` **`oriterm_mux/src/pane/io_thread/tests.rs` is 2300+ lines — readability gap, needs per-module reorganization** — found by continue-roadmap.
  Repro: `wc -l oriterm_mux/src/pane/io_thread/tests.rs` returns ~2339 lines. Test file is exempt from the 500-line hygiene limit (`.claude/rules/code-hygiene.md` §File Size), so this is NOT a BLOAT finding — but the size is a real navigation problem when finding tests for a specific subsystem.
  Subsystem: `oriterm_mux/src/pane/io_thread/tests.rs`
  Analysis: Per `.claude/rules/test-organization.md` §Sibling tests.rs Pattern, the correct reorganization is by **production module** — split along the lines of the production submodules (`commands/`, `handler.rs`, `response_poll/`, `snapshot/`, `event_proxy/`, `effect_router/`, `handle/`) into SIBLING `tests.rs` files co-located with each production module. Do NOT create an `io_thread/tests/*.rs` subdirectory — that would violate the sibling pattern. Concrete plan: (a) move tests for `response_poll` logic into `response_poll/tests.rs` (requires converting `response_poll.rs` to a directory module first); (b) move tests for `commands/` dispatch into `commands/tests.rs`; (c) move tests for `handler.rs` into `handler/tests.rs` (requires conversion); (d) move tests for `event_proxy/` into `event_proxy/tests.rs` if not already there; (e) tests for `handle.rs` into `handle/tests.rs` (requires conversion). The remaining `tests.rs` should contain only top-level `PaneIoThread::run` tests.
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Filed by effect-cutover §01.1 Cleanup per blind-spot §10. Severity is low because the file is exempt from the 500-line rule; the navigation pain is real but not a correctness issue.

- [ ] `[BUG-11-11][high]` **Daemon-mode `HostRequest` round-trip not implemented — OSC 52 / OSC 10/11/12 silently dropped on remote panes** — found by continue-roadmap.
  Repro: Connect to a remote daemon-mode pane via `MuxClient`. From the pane's shell, run `printf '\e]52;c;?\e\\'` (OSC 52 clipboard read) or `printf '\e]10;?\e\\'` (OSC 10 color query). The daemon server emits the `HostRequest` effect on its IO thread, but the request never reaches the local client — the `MuxNotification::HostClipboardLoad` / `MuxNotification::HostColorQuery` variants do not have corresponding `MuxPdu` wire variants, so the daemon `notification_to_pdu` function drops them and the requesting app never receives a reply (request hangs / times out). `MuxBackend::fulfill_host_request` on `MuxClient` returns `Err(io::Error::other("host-request fulfillment not supported on daemon backend"))` even if the request had reached the client.
  Subsystem: `oriterm_mux/src/protocol/messages.rs` (MuxPdu enum), `oriterm_mux/src/protocol/msg_type.rs` (MsgType enum + decode table), `oriterm_mux/src/protocol/pdu_traits.rs` (msg_type/is_fire_and_forget/is_notification classification), `oriterm_mux/src/server/notify/mod.rs` (server-side emission), `oriterm_mux/src/backend/client/notification.rs` (client-side translation), `oriterm_mux/src/backend/client/mod.rs` (MuxClient impl of fulfill_host_request).
  Analysis: `ResponseToken<T>` is `Arc<Mutex<Option<T>>>` — process-local, cannot cross IPC. The fix requires a request-ID + reply-PDU design: the daemon server allocates a `request_id: u64`, sends `MuxPdu::NotifyHostClipboardLoad { pane_id, request_id, selection, clipboard_char, terminator }` (no token field), the client receives it, services the request locally, and sends a reply PDU `MuxPdu::ReplyHostRequest { request_id, payload: HostReplyPayload }` back to the server, which dispatches it into the matching `pending_responses` entry on the IO thread. Same shape applies to `HostColorQuery` → `NotifyHostColorQuery` → `ReplyHostRequest`. Add `MuxPdu::NotifyDesktopNotification` + `MuxPdu::NotifyClearPendingDesktopNotifications` for the fire-and-forget side at the same time. Concrete checklist items (a)–(k) enumerated in `plans/effect-cutover/section-01-migrate-mux-consumer.md` Files block. Severity high because daemon mode is shipped functionality that silently drops OSC traffic for the entire HostRequest family.
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Filed by effect-cutover §01.4 Path B per blind-spot §2 — Section 01.4's resolution is to FILE this bug rather than land the daemon redesign atomically with the in-process effect-cutover migration. Fix should land as its own plan section or in a follow-up `daemon-host-requests` plan.

- [ ] `[BUG-11-10][medium]` **`HostEffect::PrintRequest` (MC printer passthrough — CSI ?5i / ?4i) is logged but not forwarded** — found by continue-roadmap.
  Repro: Send `printf '\e[?5i'` (start auto-print) followed by output and `printf '\e[?4i'` (stop auto-print) — no print stream is delivered. `printf '\e[5i' / '\e[4i'` (controller mode) similarly drops. VTE handler emits `HostEffect::PrintRequest(PrintRequest::*)` but the effect→MuxEvent router has no `MuxEvent::PrintRequest` variant, so the router logs at `info!` level and drops.
  Subsystem: `oriterm_mux/src/pane/io_thread/effect_router/mod.rs` (router drops with no MuxEvent), `oriterm/src/app/mux_pump/` (no handler), `oriterm_core/src/effect/families/host.rs` (`HostEffect::PrintRequest` variant).
  Analysis: ECMA-48 § Media Copy (MC, CSI Pn i) is a niche feature but real terminals (xterm, mlterm, ptyxis) implement it as either passthrough to a configured print command or as a local file dump. The fix path adds `MuxEvent::PrintData { pane_id, data: Vec<u8> }` + `MuxNotification::PrintData` and a main-thread handler that buffers print data and dispatches to `lpr` / a configured print command on stop-print. Severity medium because MC is rarely used in modern shells; severity is upgraded from low because silent drops are worse than no implementation (apps cannot detect lack of support).
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Filed by effect-cutover §01.1 Phase I per blind-spot §8.

- [ ] `[BUG-11-9][medium]` **`HostEffect::AudioRequest` (DECPS — Play Sound CSI Ps,Ps;...,Ps) is logged but not forwarded** — found by continue-roadmap.
  Repro: Send `printf '\e[1;1;100;100,~'` (or the modern DECPS `\e[Ps,Ps;...,Ps,~`) to oriterm — no sound is played. VTE handler emits `HostEffect::AudioRequest(AudioRequest::*)` but the router has no `MuxEvent::AudioRequest` variant, so the router logs at `info!` level and drops.
  Subsystem: `oriterm_mux/src/pane/io_thread/effect_router/mod.rs` (router drops with no MuxEvent), `oriterm/src/app/mux_pump/` (no handler), `oriterm_core/src/effect/families/host.rs` (`HostEffect::AudioRequest` variant).
  Analysis: DECPS (DEC Play Sound) is a VT520-era control sequence used by some BBS clients and music demos. The fix path adds `MuxEvent::AudioRequest { pane_id, frequency_hz, duration_ms, volume }` + `MuxNotification` + a main-thread handler that emits a beep through the platform audio API (CoreAudio / WASAPI / ALSA via `cpal` or similar). Severity medium for the same reason as PrintRequest — silent drops mask the gap from apps that probe with a quiet test tone.
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Filed by effect-cutover §01.1 Phase I per blind-spot §8.

- [ ] `[BUG-11-8][medium]` **`HostEffect::VisualBell` is logged but not forwarded — visual-flash mode (DECSET ?12) inert** — found by continue-roadmap.
  Repro: Set visual-bell mode via `printf '\e[?12h'`, then `printf '\007'` (BEL). No UI flash is rendered. VTE handler emits `HostEffect::VisualBell` but the router has no `MuxEvent::PaneVisualBell` variant, so the router logs at `info!` level and increments `dropped_effects_debug` (debug builds only).
  Subsystem: `oriterm_mux/src/pane/io_thread/effect_router/mod.rs` (router drops with no MuxEvent), `oriterm/src/app/mux_pump/` (no handler), `oriterm_ui/src/widgets/window_root/` (visual-flash overlay infra exists for the audible bell — mirror it).
  Analysis: Apps that prefer visual feedback over audio (terminal multiplexer in shared rooms, accessibility) set DECSET ?12 to swap audible bell for a visual flash. The audible-bell path (`HostEffect::Bell` → `MuxEvent::PaneBell` → main-thread overlay flash) already lands in §01.1; the visual-bell path is the same shape but with a different MuxEvent variant. The fix: add `MuxEvent::PaneVisualBell { pane_id }` + `MuxNotification::PaneVisualBell` + a main-thread handler that drives a brief WindowRoot tint or border flash (distinct from the audible-bell flash so the two are observable). Severity medium because the audible-bell fallback works (visual-bell users get audible noise instead of silent flash, but no data is lost).
  Found: 2026-04-18 | Source: continue-roadmap
  Note: Filed by effect-cutover §01.1 Phase I per blind-spot §8.

- [ ] `[BUG-11-7][low]` **MuxClient transport layer BLOAT: over-length functions + excessive nesting** — found by impl-hygiene-review.
  Repro: `.claude/skills/impl-hygiene-review/hygiene-lint.sh --scope oriterm_mux/src/backend/client --check fn-length,nesting-depth` reports four BLOAT findings.
  Subsystem: `oriterm_mux/src/backend/client/transport/mod.rs:93` (`fn connect` — 119 lines, limit 100); `oriterm_mux/src/backend/client/transport/reader.rs:132` (`fn reader_loop` — 112 lines, limit 100); `reader.rs:167` (`fn reader_loop` nesting depth 6, limit 4); `reader.rs:269` (`fn read_and_dispatch_frames` nesting depth 5, limit 4).
  Found: 2026-04-16 | Source: impl-hygiene-review
  Note: Surfaced during the Section 09 (DEC Private Modes) /impl-hygiene-review close-out gate. Not touched by Section 09 work — the new daemon-side bridge test in `oriterm_mux/src/backend/client/tests.rs` is clean; these findings are in `transport/` code that predates Section 09. Candidate refactors: extract PDU handshake steps out of `connect`; split `reader_loop` into `accept_frame` / `dispatch_frame` helpers; flatten the inner `if let` chains in `reader_loop` + `read_and_dispatch_frames` via guard clauses or extracted helper functions returning `Option`.

- [ ] `[BUG-11-4][high]` **DA1/DA2/DA3, DSR 5/6, CSI 18t, and DECRQM produce no response in the live terminal** — found by manual.
  Repro: Run `printf '\e[c'` (DA1), `printf '\e[5n'` (DSR 5), `printf '\e[>c'` (DA2), `printf '\e[18t'` (CSI 18t) in oriterm — no response received by the requesting process.
  Subsystem: `oriterm_mux/src/pane/io_thread/event_proxy/mod.rs` (PtyWrite forwarding), `oriterm_mux/src/in_process/event_pump.rs` (PtyWrite handling), `oriterm_mux/src/pty/mod.rs` (writer thread)
  Analysis: Code analysis shows the full pipeline appears wired: core handlers emit `Event::PtyWrite(response)` in `oriterm_core/src/term/handler/status.rs`, the IO-thread event proxy forwards these as `MuxEvent::PtyWrite`, and the event pump at `event_pump.rs:66` calls `pane.write_input()` which enqueues to the PTY writer thread. Despite the code paths existing, live testing confirms no response is received. Root cause unknown — needs runtime instrumentation (logging at each hop: event proxy send, event pump receive, writer thread write) to identify where the response is lost. Possible causes: (1) timing/ordering issue where the event pump doesn't run between the response being generated and the shell timing out its read, (2) ConPTY on Windows intercepting or dropping the response, (3) event loop wakeup not triggering `pump_mux_events()` in time, (4) some other runtime condition not visible to static analysis.
  Found: 2026-04-05 | Source: manual
  Note: Roadmap sections 02/30/38 document these handlers as implemented and verified by unit tests, but end-to-end mux round-trip was never tested in the live app. The vttest integration tests (`oriterm_core/tests/vttest/`) bypass the mux entirely.

- [ ] `[BUG-11-3][high]` **OSC 10/11/12 color queries silently dropped ��� no reply sent to requesting app** — found by manual.
  Repro: Run `printf '\e]10;?\e\\'` in oriterm ��� no response. Apps querying terminal colors (e.g., vim, neovim, delta, bat) get no reply, causing hangs or incorrect color detection.
  Subsystem: `oriterm_mux/src/pane/io_thread/event_proxy/mod.rs` (line 150 — ColorRequest dropped), `oriterm_mux/src/mux_event/tests.rs` (line 445 — test asserts no MuxEvent for ColorRequest)
  Analysis: Core correctly emits `Event::ColorRequest(index, closure)` in `oriterm_core/src/term/handler/osc.rs:94`. The closure takes the current `Rgb` color and returns the OSC response string that should be written back to the PTY. However, the IO-thread event proxy at `event_proxy/mod.rs:150` groups ColorRequest with CursorBlinkingChange and MouseCursorDirty as "events that don't need mux routing" and only wakes the event loop. The response string is never generated and never written to the PTY. The fix requires the event proxy to invoke the closure with the current color from the palette, then write the resulting response bytes back to the PTY (similar to how PtyWrite events are handled). The existing test asserting no MuxEvent for ColorRequest must also be updated.
  Found: 2026-04-05 | Source: manual
  Note: Roadmap section 38 (protocol extensions) documents OSC 10/11/12 core implementation as complete but did not verify end-to-end mux routing. Section 30 (pane domain) explicitly documented ColorRequest as "non-routed" — this was a design gap, not intentional.

- [ ] `[BUG-11-2][high]` **Memory (RSS) grows during output flooding and does not decrease after killing panes** — found by manual.
  Repro: Open multiple panes. Flood each with sustained output (e.g., `yes`, `cat /dev/urandom`). Observe RSS climbing. Kill the panes (close tabs). RSS does not decrease.
  Subsystem: `oriterm_mux/src/pane/io_thread/mod.rs`, `oriterm_mux/src/pane/io_thread/snapshot/mod.rs`, `oriterm_core/src/grid/`
  Analysis: During flooding, scrollback grows up to `max_scrollback` and the grid allocates rows. When a pane is killed, `Pane::drop()` → `PaneIoHandle::drop()` shuts down the IO thread, and `Term<T>` drop should free grid memory. Possible causes: (1) System allocator on Windows doesn't eagerly return freed pages to OS (RSS stays high even after dealloc — common with large allocations). (2) Snapshot double buffer retains large capacity allocations that aren't shrunk. (3) GPU-side buffers (instance writers, atlas entries) for killed panes aren't cleaned up. (4) Genuine leak — something holds an Arc or reference to pane data after removal.
  Found: 2026-04-01 | Source: manual
  Note: Roadmap section 50 (runtime efficiency) covers memory discipline.
  Escalated: requires plan — investigated under /fix-bug --autopilot on 2026-04-24. The bug entry lists 4 candidate root causes spanning oriterm_mux (snapshot double-buffer + cleanup_closed_pane HashMap entry removal), oriterm_core (Grid scrollback / Row recycling), and oriterm/src/gpu (atlas + instance buffers) — confirmed all 4 are plausible after reading `oriterm_mux/src/backend/embedded/mod.rs:368-377` (cleanup_closed_pane removes from HashMaps but those retain capacity; Pane drop is spawned on a background thread so memory release is asynchronous to the close action). Section 50 (runtime efficiency) is already complete and did not catch this — meaning the canonical memory-discipline pass already covered the obvious paths and this bug is in the residual 4-cause space. Cannot triage between the 4 causes from static reading alone — each requires different runtime profiling: (1) needs malloc_trim/heap-walker at process level, (2) needs allocation tracker on snapshot Vec capacities, (3) needs GPU-buffer leak audit (wgpu validation layer), (4) needs Arc-leak detection (e.g., heaptrack). Autopilot cannot run external profilers. The fix requires a plan that (a) adds runtime memory instrumentation to identify which cause is dominant, (b) implements targeted fixes per cause (could be allocator hint, explicit shrink_to_fit, GPU buffer release, or Arc audit), and (c) extends `oriterm_core/tests/rss_regression.rs` to include the pane-kill-RSS-shrink scenario.

- [ ] `[BUG-11-5][medium]` **signal_child() sends SIGINT to shell PGID instead of foreground PGID** — found by tpr-review.
  Repro: Spawn a pane, run `yes` (foreground job). Call `signal_child(Signal::Interrupt)`. SIGINT goes to the shell's process group, not the foreground process group running `yes`.
  Subsystem: `oriterm_mux/src/pane/mod.rs:442` (`send_signal_platform`)
  Analysis: `kill(-pid, SIGINT)` uses the shell PID as the process group ID. This works when the shell IS the foreground process, but misses foreground jobs (`yes`, `cat`, etc.) that run in their own process group. The fix is to use `tcgetpgrp(pty_fd)` to obtain the foreground PGID and signal that instead. Two e2e tests are `#[ignore]` waiting for this: `e2e.rs:1487` and `e2e.rs:1654`.
  Found: 2026-04-05 | Source: tpr-review
  Note: Keyboard Ctrl+C (BUG-11-1) works correctly — the kernel PTY layer handles `\x03` → SIGINT to the foreground PGID. This bug only affects the programmatic `signal_child()` path.

- [ ] `[BUG-11-6][medium]` **`push_notification_triggers_dirty_flag` e2e test flaky — asserts on first dirty snapshot**
  Repro: `cargo test -p oriterm_mux --test e2e push_notification_triggers_dirty_flag` in a loop; fails intermittently.
  Subsystem: `oriterm_mux/tests/e2e.rs:280`
  Analysis: Test sends `echo PUSH_TEST\n`, waits for `is_pane_snapshot_dirty`, then asserts the refreshed snapshot contains "PUSH_TEST". The snapshot can become dirty from the command echo or prompt redraw before the actual output lands. Fix: continue the loop when snapshot doesn't contain "PUSH_TEST" instead of asserting on the first dirty snapshot.
  Found: 2026-04-12 | Source: continue-roadmap

## Resolved Bugs

- [x] `[BUG-11-1][critical]` **All input blocked during sustained output flooding (even single pane)** — found by manual.
  Resolved: 2026-04-05. Two-part fix: (1) PTY writer thread now sets a `write_stalled` AtomicBool flag before potentially-blocking `write()` calls — the main thread reads this to detect when the writer is stuck on a full kernel buffer. (2) When Ctrl+C is pressed and the writer is stalled, SIGINT is sent directly to the child process group via `kill(-pid, SIGINT)` on Unix / `GenerateConsoleCtrlEvent` on Windows, bypassing the blocked PTY pipe. Writer thread also improved: coalesces pending input, uses `recv_timeout` instead of blocking `recv`, and flushes pending data before shutdown. Files: `oriterm_mux/src/pty/mod.rs` (writer thread), `oriterm_mux/src/pane/mod.rs` (Signal enum, signal delivery), `oriterm_mux/src/backend/embedded/mod.rs` + `mod.rs` (MuxBackend trait), `oriterm/src/app/keyboard_input/mod.rs` (Ctrl+C bypass).
