---
schema_version: "1.0"
stack: osc
title: "OSC Suite Catalog"
owner_section: "01 (bootstrap), 10 (verification)"
---

# OSC Suite Catalog

One row per OSC numeric sub-op dispatched by `crates/vte/src/ansi/dispatch/osc.rs::dispatch`. OSC 1337 (iTerm2) lives in `iterm2.md`. OSC terminator is BEL (0x07) or ST (`ESC \\`); the dispatcher selects the echoed form via `bell_terminated`.

Section 01 populates rows for every dispatch arm currently implemented. Section 10 (OSC Suite full) verifies each chain end-to-end and adds OSC 9 / 99 / 133 / 633 / 777 when their dispatch arms land.

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| OSC-0 | xterm ctlseqs (OSC 0) | `` `OSC 0 ; Pt BEL\|ST` `` | Set icon name and window title | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"0"` arm → `Term::set_title` + `Term::set_icon_name` (`oriterm_core/src/term/handler/osc.rs`) `` | effect-host-title | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/basic.rs::{osc0_sets_title_and_icon, osc0_empty_sets_empty_string, osc0_bel_and_st_terminators_both_accepted}` | verified | — | Shared dispatch arm with OSC 1 and OSC 2. Empty payload (`OSC 0 ; ST`) stores `""`, not `None` — no `ResetTitle` path. |
| OSC-1 | xterm ctlseqs (OSC 1) | `` `OSC 1 ; Pt BEL\|ST` `` | Set icon name only | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1"` arm → `Term::set_icon_name` (`oriterm_core/src/term/handler/osc.rs`) `` | effect-host-title | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/basic.rs::osc1_sets_only_icon_name` | verified | — | Title field is NOT touched (negative pin). |
| OSC-2 | xterm ctlseqs (OSC 2) | `` `OSC 2 ; Pt BEL\|ST` `` | Set window title only | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"2"` arm → `Term::set_title` (`oriterm_core/src/term/handler/osc.rs`) `` | effect-host-title | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/basic.rs::osc2_sets_only_title` | verified | — | Icon name is NOT touched (negative pin). |
| OSC-4-SET | xterm ctlseqs (OSC 4) | `` `OSC 4 ; Ps ; spec BEL\|ST` `` | Set palette index to color spec (`rgb:RR/GG/BB` etc.) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"4"` arm → `xparse_color` → `Term::set_color` (`oriterm_core/src/term/handler/osc.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/palette.rs::{osc4_set_palette_index, osc4_multi_param_sets_multiple_indices, osc4_out_of_range_dropped, osc4_invalid_color_dropped}` | verified | — | Marks grid dirty when non-cursor index changes. Out-of-range indices (≥ `NUM_COLORS`) are silently bounds-checked by `Palette::set`; unparseable color specs route to `unhandled` without mutating state. |
| OSC-4-QUERY | xterm ctlseqs (OSC 4) | `` `OSC 4 ; Ps ; ? BEL\|ST` `` | Query palette index → `OSC 4 ; Ps ; rgb:…` response | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"4"` arm with `b"?"` → `Term::dynamic_color_sequence` (`oriterm_core/src/term/handler/osc.rs`) — emits `Effect::HostRequest(HostRequest::ColorQuery { prefix: "4;Ps", index: Ps, .. })` `` | effect-pty-write | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/palette.rs::osc4_query_palette_index` | verified | — | Dispatcher builds `prefix = "4;{index}"` so both OSC number and palette index survive into the reply formatter. Reply bytes (`OSC 4 ; Ps ; rgb:… ST`) formatted by the mux consumer — out of spec_chain scope. |
| OSC-7 | iTerm2 / VTE de-facto | `` `OSC 7 ; Pt BEL\|ST` `` | Report current working directory (URI) | `` `RawInterceptor::handle_osc7` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"7"` arm → `parse_osc7_path` → `percent_decode` → `Term::set_cwd` (SSOT) + emits `Effect::Host(HostEffect::CwdSet { cwd })` via `QueueingEffectSink` `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc7_file_uri_sets_cwd, osc7_file_uri_with_hostname, osc7_percent_decoded, osc7_emits_host_effect_cwd_set, osc7_relative_path_passed_through, osc7_via_high_level_processor_drops, interceptor_osc7_sets_cwd, interceptor_osc7_empty_hostname, interceptor_osc7_path_parsing, interceptor_osc7_percent_encoded_space}` | verified | wezterm escape-sequences.md | Interceptor-only path. The vestigial `b"7"` arm in `crates/vte/src/ansi/dispatch/osc.rs` was deleted by Section 10.8 — it called a no-op `Handler::set_working_directory` default and provided no value. The interceptor parses the URI (stripping the hostname segment), percent-decodes the path, and updates `Term::set_cwd`. Negative pin `osc7_via_high_level_processor_drops` confirms the high-level `Processor` does NOT route OSC 7 (preventing double-dispatch regression). |
| OSC-8 | Gnome / Final Term OSC 8 | `` `OSC 8 ; params ; URI BEL\|ST` `` | Hyperlink begin / end | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"8"` arm → `Term::set_hyperlink` → `Term::osc_set_hyperlink` (`oriterm_core/src/term/handler/osc.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/hyperlinks.rs::{osc8_basic_attach, osc8_with_id, osc8_survives_reflow, osc8_survives_scrollback, osc8_terminator_cancels_attachment, osc8_malformed_uri_dropped, osc8_alt_screen_toggle_clears, osc8_renderable_observer_not_stub}` | verified | wezterm escape-sequences.md | Only `id=<id>` key is parsed. Empty URI ends the hyperlink. URI survives reflow, scrollback, and alt-screen toggle (primary unaffected). |
| OSC-10-SET | xterm ctlseqs (OSC 10) | `` `OSC 10 ; spec BEL\|ST` `` | Set default foreground color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"10"` arm (shared with 11, 12) → `Term::set_color` → `Palette::foreground` (`NamedColor::Foreground` = 256) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/default_colors.rs::{osc10_sets_default_foreground, osc10_multi_param_walks_named_colors}` | verified | — | Multi-param form walks `NamedColor::Foreground..=Cursor` via `dynamic_code += 1` at line 171. |
| OSC-10-QUERY | xterm ctlseqs (OSC 10) | `` `OSC 10 ; ? BEL\|ST` `` | Query default foreground | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"10"` query form → `Term::dynamic_color_sequence` — emits `Effect::HostRequest(HostRequest::ColorQuery { prefix: "10", index: 256, .. })` `` | effect-pty-write | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/default_colors.rs::osc10_query_replies_rgb` | verified | — | Prefix is bare OSC number (stringified); index maps via `NamedColor::Foreground + (dynamic_code - 10)`. |
| OSC-11-SET | xterm ctlseqs (OSC 11) | `` `OSC 11 ; spec BEL\|ST` `` | Set default background color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"11"` arm → `Term::set_color` → `Palette::background` (`NamedColor::Background` = 257) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/default_colors.rs::osc11_sets_default_background` | verified | — | |
| OSC-11-QUERY | xterm ctlseqs (OSC 11) | `` `OSC 11 ; ? BEL\|ST` `` | Query default background | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"11"` query form → `Term::dynamic_color_sequence` — emits `Effect::HostRequest(HostRequest::ColorQuery { prefix: "11", index: 257, .. })` `` | effect-pty-write | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::osc_reset_round_trip_with_query` | verified | — | Shared dispatch apex with OSC-10-QUERY / OSC-12-QUERY — pin verifies prefix/index via the color_reset round-trip matrix. |
| OSC-12-SET | xterm ctlseqs (OSC 12) | `` `OSC 12 ; spec BEL\|ST` `` | Set text cursor color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"12"` arm → `Term::set_color` → `Palette::cursor_color` (`NamedColor::Cursor` = 258) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/default_colors.rs::osc12_sets_cursor_color` | verified | — | |
| OSC-12-QUERY | xterm ctlseqs (OSC 12) | `` `OSC 12 ; ? BEL\|ST` `` | Query text cursor color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"12"` query form → `Term::dynamic_color_sequence` — emits `Effect::HostRequest(HostRequest::ColorQuery { prefix: "12", index: 258, .. })` `` | effect-pty-write | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::osc_reset_round_trip_with_query` | verified | — | Shared dispatch apex with OSC-10-QUERY / OSC-11-QUERY. |
| OSC-22 | xterm ctlseqs (OSC 22) | `` `OSC 22 ; Pt BEL\|ST` `` | Set mouse cursor shape | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"22"` arm → `Handler::set_mouse_cursor_icon` override on `Term` (`oriterm_core/src/term/handler/mod.rs`) → `Term::set_mouse_cursor_icon` field (`oriterm_core/src/term/mod.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/cursor.rs::{osc22_pointer_sets_cursor_icon, osc22_all_known_icons_matrix, osc22_unknown_icon_is_dropped, osc22_no_parameter_is_dropped, osc22_reset_behavior, osc22_does_not_affect_text_cursor_shape}`; daemon wire: `oriterm_mux/src/server/snapshot/tests.rs::{osc22_daemon_snapshot_carries_cursor_icon, daemon_snapshot_no_osc22_yields_none, daemon_snapshot_encodes_every_wire_icon}`; client decode: `oriterm/src/gpu/extract/from_snapshot/tests.rs::{snapshot_to_renderable_populates_mouse_cursor_icon, snapshot_to_renderable_into_populates_mouse_cursor_icon, snapshot_to_renderable_none_icon_stays_none}` | verified | — | Dispatcher parses via `cursor_icon::CursorIcon::from_str` and calls the `Handler` override on `Term`, which stores the icon in the `mouse_cursor_icon: Option<CursorIcon>` field. Exposed to the renderer via `RenderableContent::mouse_cursor_icon` (embedded) and `PaneSnapshot::mouse_cursor_icon` encoded as a stable `u8` wire index (daemon). Unknown cursor names and malformed `params.len() != 2` sequences drop without mutating state. |
| OSC-50 | xterm ctlseqs (OSC 50) | `` `OSC 50 ; CursorShape=Ps BEL\|ST` `` | Set cursor shape (legacy URxvt/rxvt form) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"50"` arm → `Term::set_cursor_shape` (`oriterm_core/src/term/handler/dcs.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/cursor.rs::{osc50_cursor_shape_block, osc50_cursor_shape_beam, osc50_cursor_shape_underline, osc50_unknown_shape_dropped, osc50_malformed_prefix_dropped}` | verified | — | Parses the `CursorShape=N` prefix; shape 0/1/2 = block/beam/underline. `vte::ansi::CursorShape::Beam` maps to `oriterm_core::CursorShape::Bar` via the `From` impl. Unknown `N` values and a missing `CursorShape=` prefix drop without mutating `Term::cursor_shape`. |
| OSC-52-STORE | xterm ctlseqs (OSC 52) | `` `OSC 52 ; Pc ; <b64> BEL\|ST` `` | Store clipboard content (base64) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"52"` arm → `Term::clipboard_store` → `Term::osc_clipboard_store` (`oriterm_core/src/term/handler/osc.rs`) — emits `Effect::Host(HostEffect::ClipboardStore { selection, data })` via `QueueingEffectSink` (`oriterm_core::effect::HostEffect`) `` | effect-clipboard | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/clipboard.rs::{osc52_store_clipboard_c, osc52_store_clipboard_s, osc52_store_clipboard_p, osc52_store_clipboard_q_dropped, osc52_store_invalid_base64_dropped}` | verified | — | `Pc` ∈ {`c`, `s`, `p`} route to `ClipboardSelection::{Clipboard, Select, Primary}`; `q` has no variant and is dropped silently. Invalid base64 and non-UTF-8 payloads are dropped without emitting any effect. |
| OSC-52-LOAD | xterm ctlseqs (OSC 52) | `` `OSC 52 ; Pc ; ? BEL\|ST` `` | Request clipboard content | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"52"` arm with `b"?"` → `Term::clipboard_load` → `Term::osc_clipboard_load` (`oriterm_core/src/term/handler/osc.rs`) — emits `Effect::HostRequest(HostRequest::ClipboardLoad { selection, clipboard_char, terminator, reply: ResponseToken })` via `QueueingEffectSink` `` | effect-clipboard | parser:pass dispatch:pass effect:pass — `oriterm_core/tests/spec_chain/osc/clipboard.rs::{osc52_load_request_fires_hostrequest, osc52_load_with_s_and_p_selections}` + `oriterm_mux/src/pane/io_thread/response_poll/tests.rs::osc52_register_poll_roundtrip` + `crates/oriterm_test_support/src/session/pty_responder/tests.rs::osc52_embedded_backend_fulfills_via_session_pty_responder` | verified | — | `ResponseToken` fulfillment reaches `PtyEffect::Write` via `PaneIoThread::poll_pending_responses` (daemon path) and via `PtyResponder` (embedded path). Reply bytes are formatted by `oriterm_core::effect::format_clipboard_reply` — the SSOT formatter both consumers use. |
| OSC-104 | xterm ctlseqs (OSC 104) | `` `OSC 104 [; Ps]* BEL\|ST` `` | Reset palette entries (zero-arg = reset all 256) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"104"` arm → `Term::reset_color` → `Term::osc_reset_color` (`oriterm_core/src/term/handler/osc.rs`) — `Palette::reset_indexed` restores `defaults[index]`; non-cursor reset marks grid dirty `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::{osc104_zero_args_resets_all_256_palette, osc104_specific_indices_resets_only_those, osc104_invalid_index_dropped, osc104_reset_marks_grid_dirty}` | verified | — | Zero-arg form resets indices 0..=255; explicit indices reset only those. Non-numeric tokens route to `unhandled` without mutating state; out-of-range indices are silently bounds-checked by `Palette::reset_indexed`. |
| OSC-110 | xterm ctlseqs (OSC 110) | `` `OSC 110 BEL\|ST` `` | Reset default foreground color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"110"` arm → `Term::reset_color(NamedColor::Foreground)` → `Term::osc_reset_color` (`oriterm_core/src/term/handler/osc.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::{osc110_resets_default_foreground, osc_reset_round_trip_with_query}` | verified | — | Zero-arg form only. Restores index 256 (`NamedColor::Foreground`) to the value captured at `Palette::for_theme(Theme::default())` construction; post-reset OSC 10 `;?` query still emits `HostRequest::ColorQuery`. |
| OSC-111 | xterm ctlseqs (OSC 111) | `` `OSC 111 BEL\|ST` `` | Reset default background color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"111"` arm → `Term::reset_color(NamedColor::Background)` → `Term::osc_reset_color` (`oriterm_core/src/term/handler/osc.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::{osc111_resets_default_background, osc_reset_round_trip_with_query}` | verified | — | Restores index 257 (`NamedColor::Background`) to the theme default; post-reset OSC 11 `;?` query still emits `HostRequest::ColorQuery`. |
| OSC-112 | xterm ctlseqs (OSC 112) | `` `OSC 112 BEL\|ST` `` | Reset text cursor color | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"112"` arm → `Term::reset_color(NamedColor::Cursor)` → `Term::osc_reset_color` (`oriterm_core/src/term/handler/osc.rs`) `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_core/tests/spec_chain/osc/color_reset.rs::{osc112_resets_cursor_color, osc_reset_round_trip_with_query}` | verified | — | Restores index 258 (`NamedColor::Cursor`) to the theme default. Cursor index is the sole `reset_color` path that does NOT mark the grid dirty — matches `Term::osc_reset_color`'s non-cursor-only dirty guard. |
| OSC-13-SET | xterm ctlseqs (OSC 13) | `` `OSC 13 ; spec BEL\|ST` `` | Set mouse foreground color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Paired with OSC 113 (reset). |
| OSC-13-QUERY | xterm ctlseqs (OSC 13) | `` `OSC 13 ; ? BEL\|ST` `` | Query mouse foreground color | MISSING — to be added by Section 10 (OSC Suite) | effect-pty-write | parser:pending dispatch:pending effect:pending | missing | — | |
| OSC-14-SET | xterm ctlseqs (OSC 14) | `` `OSC 14 ; spec BEL\|ST` `` | Set mouse background color (Tektronix) | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Paired with OSC 114 (reset). |
| OSC-14-QUERY | xterm ctlseqs (OSC 14) | `` `OSC 14 ; ? BEL\|ST` `` | Query mouse background color (Tektronix) | MISSING — to be added by Section 10 (OSC Suite) | effect-pty-write | parser:pending dispatch:pending effect:pending | missing | — | |
| OSC-17-SET | xterm ctlseqs (OSC 17) | `` `OSC 17 ; spec BEL\|ST` `` | Set highlight background color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Paired with OSC 117 (reset). Selection highlight bg. |
| OSC-17-QUERY | xterm ctlseqs (OSC 17) | `` `OSC 17 ; ? BEL\|ST` `` | Query highlight background color | MISSING — to be added by Section 10 (OSC Suite) | effect-pty-write | parser:pending dispatch:pending effect:pending | missing | — | |
| OSC-19-SET | xterm ctlseqs (OSC 19) | `` `OSC 19 ; spec BEL\|ST` `` | Set highlight foreground color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Paired with OSC 119 (reset). Selection highlight fg. |
| OSC-19-QUERY | xterm ctlseqs (OSC 19) | `` `OSC 19 ; ? BEL\|ST` `` | Query highlight foreground color | MISSING — to be added by Section 10 (OSC Suite) | effect-pty-write | parser:pending dispatch:pending effect:pending | missing | — | |
| OSC-113 | xterm ctlseqs (OSC 113) | `` `OSC 113 BEL\|ST` `` | Reset mouse foreground color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Counterpart of OSC 13. |
| OSC-114 | xterm ctlseqs (OSC 114) | `` `OSC 114 BEL\|ST` `` | Reset mouse background color (Tektronix) | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Counterpart of OSC 14. |
| OSC-117 | xterm ctlseqs (OSC 117) | `` `OSC 117 BEL\|ST` `` | Reset highlight background color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Counterpart of OSC 17. |
| OSC-119 | xterm ctlseqs (OSC 119) | `` `OSC 119 BEL\|ST` `` | Reset highlight foreground color | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | — | No dispatch arm. Counterpart of OSC 19. |
| OSC-3 | xterm ctlseqs (OSC 3) | `` `OSC 3 ; prop ; value BEL\|ST` `` | Set X11 window property | MISSING — to be added by Section 10 (OSC Suite) | effect-host-title | parser:pending dispatch:pending effect:pending | missing | wezterm escape-sequences.md | Both wezterm and ori_term currently ignore. Platform-specific (X11-only). |
| OSC-5 | xterm ctlseqs (OSC 5) | `` `OSC 5 ; Ps ; spec BEL\|ST` `` | Change/query special color (highlight / bold / etc.) | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | wezterm escape-sequences.md | No dispatch arm. |
| OSC-6 | iTerm2 proprietary (OSC 6) | `` `OSC 6 ; Pt BEL\|ST` `` | Change title tab color (iTerm2) | MISSING — to be added by Section 10 (OSC Suite) | state-snapshot | parser:pending dispatch:pending state:pending | missing | wezterm escape-sequences.md | |
| OSC-9 | iTerm2 notifications | `` `OSC 9 ; Pt BEL\|ST` `` | Growl-style notification | `` `RawInterceptor::handle_notification_simple` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"9"` arm → `Effect::Host(HostEffect::DesktopNotification { source: NotificationSource::Osc9, title: "", body })` via `QueueingEffectSink` (`oriterm_core::effect::HostEffect`) `` | effect-host-notification | parser:pass dispatch:pass effect:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc9_simple_body_fires_notification, osc9_empty_body, osc9_and_osc99_use_different_sources, osc9_via_processor_without_mux_drops}` | verified | wezterm escape-sequences.md | Routed by the mux interceptor — the high-level `vte::ansi::Processor` does NOT route OSC 9. OSC 9 has no title field (Growl-style). |
| OSC-99 | kitty notification protocol | `` `OSC 99 ;; Pt BEL\|ST` `` (no metadata) or `` `OSC 99 ; metadata ; Pt BEL\|ST` `` (with metadata) | Kitty desktop-notification protocol — payload type discriminated by `p=` metadata key (default `p=title`) | `` `RawInterceptor::handle_notification_simple` + `parse_osc99_payload_kind` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"99"` arm → `Effect::Host(HostEffect::DesktopNotification { source: NotificationSource::Osc99, title, body })` via `QueueingEffectSink`; `p=title` (default) routes payload at `params[2]` into `title`, `p=body` routes into `body` `` | effect-host-notification | parser:pass dispatch:pass effect:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc99_default_payload_routes_to_title, osc99_metadata_form_default_p_routes_payload_to_title, osc99_p_body_routes_payload_to_body, osc99_empty_payload_drops_notification, osc99_unsupported_payload_kind_drops_notification, osc9_and_osc99_use_different_sources, interceptor_osc99_kitty_notification}` | verified-with-deviation | kitty terminal docs (`docs/desktop-notifications.rst`) | Per Kitty's OSC 99 spec the two semicolons are mandatory even when metadata is empty (`OSC 99 ;; payload ST`); payload extracted from `params[2]`. Honoured: `p=title` (default), `p=body`. Per spec "Terminal emulators should ignore payloads of unknown type" — `p=close`, `p=icon`, `p=?`, `p=alive`, `p=buttons`, and any unknown value drop the notification. Per spec "A notification with not title and no body is ignored" — empty payload also drops. Deviation: every metadata key OTHER than `p` (`i=ID` chunk id, `d=0/1` done flag, `e=1` base64-encoded, `t=TYPE` notification type, `f=APP` application name, `n=ICON` icon name, `o=URGENCY-ON`, `s=SOUND`, `u=URGENCY`, etc.) is recognised as opaque and silently discarded — chunking, base64, urgency, sound, and notification-type filtering are NOT honoured. The "title falls back to body" rule (Kitty: "If a notification has no title, the body will be used as title") is left to the consumer-side renderer; the producer always emits title/body separately. |
| OSC-L | Sun console extension | `` `OSC L Pt BEL\|ST` `` (single-letter sub-op) | Set icon name (Sun console alias for OSC 1) | MISSING — to be added by Section 10 (OSC Suite) | effect-host-title | parser:pending dispatch:pending effect:pending | missing | wezterm escape-sequences.md | Historical Sun console extension. |
| OSC-l | Sun console extension | `` `OSC l Pt BEL\|ST` `` | Set window title (Sun console alias for OSC 2) | MISSING — to be added by Section 10 (OSC Suite) | effect-host-title | parser:pending dispatch:pending effect:pending | missing | wezterm escape-sequences.md | Historical Sun console extension. |
| OSC-133-PROMPT | Final Term semantic prompt | `` `OSC 133 ; {A\|B\|C} ; params BEL\|ST` `` | Semantic prompt markers A/B/C (prompt / command / output start) | `` `RawInterceptor::handle_osc133` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"133"` arm, `A`/`B`/`C` letters set `PromptState` and pending deferred-mark flags via `Term::set_prompt_state`, `Term::set_prompt_mark_pending`, `Term::set_command_start_mark_pending`, `Term::set_output_start_mark_pending` `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc133_a_sets_prompt_state, osc133_b_sets_command_state, osc133_c_sets_output_state, osc133_a_without_b_does_not_record_command, osc133_full_lifecycle_records_markers, osc133a_via_processor_only_does_not_change_prompt_state}` | verified | wezterm escape-sequences.md | Interceptor-only — the high-level `vte::ansi::Processor` does NOT route OSC 133. `PromptMarker { prompt, command, output }` has NO fourth field; `D` lives in the separate `OSC-133-CMD-COMPLETE` row. Deferred marks populated by `Term::mark_prompt_row` / `mark_command_start_row` / `mark_output_start_row`. |
| OSC-133-CMD-COMPLETE | Final Term semantic prompt | `` `OSC 133 ; D [; exitcode] BEL\|ST` `` | Command complete — clears `PromptState` and emits `HostEffect::CommandComplete { duration }` | `` `RawInterceptor::handle_osc133` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"133"` arm, `D` letter calls `Term::finish_command(None)` and pushes `Effect::Host(HostEffect::CommandComplete { duration })` via `QueueingEffectSink` `` | effect-host-command | parser:pass dispatch:pass effect:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc133_d_clears_state_and_emits_command_complete, osc133_command_complete_without_c_is_noop, osc133d_computes_command_duration, osc133d_without_c_produces_no_duration, command_duration_updates_on_new_command, interceptor_osc133d_with_exit_code_zero, interceptor_osc133d_with_negative_exit_code, interceptor_osc133d_with_exit_code_and_aid}` | verified | wezterm escape-sequences.md | Duration uses the Option A timing seam (`Term::finish_command(now: Option<Instant>)`); production calls with `None`, tests inject deterministic `Instant`. Exit code and key=value options after `D` are tolerated (dispatch keys only on `params[1][0]`). `D` without preceding `C` emits NO effect per `finish_command()` returning `None` when `command_start` is unset. |
| OSC-633 | VS Code shell integration | `` `OSC 633 ; {A\|B\|C\|D\|E\|P} ; params BEL\|ST` `` | VS Code shell integration — A/B/C/D mirror OSC 133 semantics; `E;<line>` stores raw command line on `Term::last_command_line`; `P;Cwd=<path>` routes through `Term::set_cwd` (SSOT with OSC 7) | `` `RawInterceptor::handle_osc633` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"633"` arm delegates A/B/C/D to the OSC 133 state machine, `E` calls `Term::set_last_command_line`, `P;Cwd=` calls `Term::set_cwd` + `Term::mark_title_dirty` `` | state-snapshot | parser:pass dispatch:pass state:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc633_a_sets_prompt_state, osc633_b_sets_command_state, osc633_c_sets_output_state, osc633_d_emits_command_complete, osc633_e_records_command_line, osc633_p_cwd_sets_term_cwd, osc633_p_unknown_key_dropped, osc633_via_high_level_processor_drops}` | verified | `src/vs/platform/terminal/common/xterm/shellIntegrationAddon.ts` | Interceptor-only dispatch — the high-level `vte::ansi::Processor` does NOT route OSC 633 (negative pin `osc633_via_high_level_processor_drops`). Unknown `P` keys (`IsWindows`, `Task`, ...) are recognised as opaque and silently discarded. |
| OSC-777 | urxvt notifications | `` `OSC 777 ; notify ; Pt BEL\|ST` `` | urxvt notification / perl extension dispatch | `` `RawInterceptor::handle_notification_777` (`oriterm_mux/src/shell_integration/interceptor.rs`) — `b"777"` arm → `Effect::Host(HostEffect::DesktopNotification { source: NotificationSource::Osc777, title, body })` via `QueueingEffectSink` `` | effect-host-notification | parser:pass dispatch:pass effect:pass — `oriterm_mux/src/shell_integration/tests.rs::{osc777_notify_title_body, osc777_non_notify_action_dropped, osc777_missing_title}` | verified | wezterm escape-sequences.md | Only `notify` action dispatches; other actions are filtered out. Title is `params[2]`, body is `params[3]`; both default to empty when absent. |
