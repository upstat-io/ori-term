---
schema_version: "1.0"
stack: kittygfx
title: "Kitty Graphics Protocol Catalog"
owner_section: "01 (bootstrap), 13 (verification)"
---

# Kitty Graphics Protocol Catalog

APC `G` commands: transmit / place / delete / animate / query / compose / frame. Kitty is the de facto spec; rows here mirror every documented action, transmission medium, format, compression, delete specifier, frame-action, animate-action, response code, and Unicode placeholder arm.

Dispatch entry: `Performer::apc_start` + `apc_put` + `apc_end` (`crates/vte/src/ansi/dispatch/mod.rs`) → `Term::apc_dispatch` → `Term::handle_apc_dispatch` (`oriterm_core/src/term/handler/image/mod.rs`) dispatches on payload first byte (`G` = Kitty) → `Term::handle_kitty_graphics` (`oriterm_core/src/term/handler/image/kitty/mod.rs`). Actions are parsed via `KittyAction` (`oriterm_core/src/image/kitty/parse.rs`) and routed to the per-action submodules under `oriterm_core/src/term/handler/image/kitty/{transmit,place,delete/mod,frame,animate,query,response,store}.rs` (post-§13.0.5 split).

## Core Actions

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-ACTION-TRANSMIT` | kitty graphics-protocol.rst §Transmitting image data | `` `APC G a=t,...; <data> ST` `` | Transmit (a=t) — upload image bytes without placement | `` `Term::kitty_transmit` (`oriterm_core/src/term/handler/image/kitty/transmit.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 pins parser→dispatch→snapshot via `kitty_action_transmit_stores_image_without_placement` in `oriterm_core/tests/spec_chain/kitty/actions.rs`. Chunked variant rides `more_data` — §13.2 owns. GPU texture-render rung owned by §13.4/§13.6. |
| `KG-ACTION-TRANSMIT-AND-PLACE` | kitty graphics-protocol.rst §Transmitting image data | `` `APC G a=T,...; <data> ST` `` | Transmit and place (a=T) — upload + immediate placement | `` `Term::kitty_transmit_and_place` (`oriterm_core/src/term/handler/image/kitty/transmit.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | `a=T` is parsed as the fallback arm (`parse.rs` `apply_key_value` for key `a`), not a distinct match arm; audit pins this via `KG-ACTION-FALLBACK-TRANSMITANDPLACE`. §13.1 pins parser→dispatch→snapshot via `kitty_action_transmit_and_place_creates_placement`. GPU texture-render rung owned by §13.4/§13.6. |
| `KG-ACTION-PLACE` | kitty graphics-protocol.rst §Displaying an already transmitted image | `` `APC G a=p,i=<id>,p=<pid>,...; ST` `` | Place (a=p) — create placement for an existing image | `` `Term::kitty_place` (`oriterm_core/src/term/handler/image/kitty/place.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | U=1 defers placement to Unicode placeholder cells (§13.4 owns that arm). §13.1 pins parser→dispatch→snapshot via `kitty_action_place_creates_placement_for_prior_transmit`. GPU texture-render rung owned by §13.4/§13.6. |
| `KG-ACTION-DELETE` | kitty graphics-protocol.rst §Deleting images | `` `APC G a=d,d=<spec>,...; ST` `` | Delete (a=d) — aggregate action; per-specifier rows pin individual semantics | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty/delete/mod.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | BUG-08-7 + BUG-08-8 closed by §13.0.5. Delete split into per-action submodule; per-specifier semantics pinned by `kitty/delete/tests.rs`. |
| `KG-ACTION-FRAME` | kitty graphics-protocol.rst §Animation | `` `APC G a=f,i=<id>,...; <data> ST` `` | Frame (a=f) — append/replace/edit animation frame | `` `Term::kitty_frame` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Per-frame-action rows (`KG-FRAME-*`) split the TRANSMIT vs REPLACE vs EDIT vs COMPOSITE-* semantics. |
| `KG-ACTION-ANIMATE` | kitty graphics-protocol.rst §Animation control | `` `APC G a=a,i=<id>,s=<n>,v=<n>,...; ST` `` | Animate (a=a) — playback + loop + frame-gap control | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Per-animate-action rows (`KG-ANIMATE-*`) split by sub-operation. |
| `KG-ACTION-QUERY` | kitty graphics-protocol.rst §Querying for image support | `` `APC G a=q,i=<id>,...; ST` `` | Query (a=q) — respond with OK without state mutation | `` `Term::kitty_query` (`oriterm_core/src/term/handler/image/kitty/query.rs`) `` | effect-pty-write | parser:verified dispatch:verified effect:verified | verified-with-deviation | wezterm `term/src/terminalstate/kitty.rs` | Currently hardcodes `OK`; does not validate payload correctness — deviation pinned by `kitty_action_query_emits_ok_reply_without_state_change`. §13.1 pins parser→dispatch→effect via that same test. |
| `KG-ACTION-COMPOSE` | kitty graphics-protocol.rst §Animation frame composition | `` `APC G a=c,i=<id>,c=<frame>,r=<frame>,...; ST` `` | Compose (a=c) — compose one frame onto another via source/dest rect blend | MISSING — to be added by §13.3 (or as a distinct `kitty/compose.rs` submodule) | texture-render | parser:missing dispatch:missing snapshot:missing | missing | wezterm `term/src/terminalstate/kitty.rs` | `KittyAction` has no variant; `apply_key_value` in `parse.rs` silently falls back to `TransmitAndPlace`. Companion row: `KG-ACTION-FALLBACK-TRANSMITANDPLACE`. |
| `KG-ACTION-FALLBACK-TRANSMITANDPLACE` | kitty graphics-protocol.rst §Transmitting image data (behavioral pin) | `` `APC G a=<unknown>,...; ST` `` | Unknown/unsupported `a=` values silently parse as `TransmitAndPlace` | `` `apply_key_value` (`oriterm_core/src/image/kitty/parse.rs`) `` | parser-only | parser:verified | verified-with-deviation | wezterm `term/src/terminalstate/kitty.rs` | Load-bearing fallback for `a=T` itself; also swallows `a=c` and future action codes. §13.1 pins semantic + negative via `kitty_action_fallback_transmit_and_place_on_unknown_a_value` + `kitty_action_unknown_a_value_does_not_route_to_transmit` — any future `/fix-bug` that narrows the fallback arm trips both. |

## Transmission

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-TRANSMIT-DIRECT` | kitty graphics-protocol.rst §Transmission mediums | `` `APC G a=t,t=d,...; <base64-data> ST` `` | Direct transmission — payload is base64-encoded image bytes | `` `Term::kitty_store_image` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Default when `t=` is absent; `t=<unknown>` also falls back to Direct per `parse.rs` `apply_key_value`. §13.1 pins via `kitty_transmission_direct_stores_inline_payload`. |
| `KG-TRANSMIT-FILE` | kitty graphics-protocol.rst §Transmission mediums | `` `APC G a=t,t=f,...; <base64-path> ST` `` | File transmission — payload is a base64-encoded regular-file path | `` `Term::kitty_store_from_file` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Rejects `..` parent-dir traversal via `path.components()` inspection (not string matching); resolves via `std::fs::read`. §13.1 pins success path via `kitty_transmission_file_reads_from_disk_without_removing_source` + traversal guard via `kitty_transmission_file_path_traversal_rejected`. |
| `KG-TRANSMIT-TEMPFILE` | kitty graphics-protocol.rst §Transmission mediums | `` `APC G a=t,t=t,...; <base64-path> ST` `` | Temp-file transmission — terminal reads then deletes the file | `` `Term::kitty_store_from_file` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Deletes path on success AND on oversize-reject. §13.1 pins source-file-removal invariant via `kitty_transmission_tempfile_reads_and_removes_source`. |
| `KG-TRANSMIT-SHARED-MEM-REJECTED` | kitty graphics-protocol.rst §Transmission mediums (`t=s`) | `` `APC G a=t,t=s,...; <shm-name> ST` `` | Shared-memory transmission — ori_term rejects with EINVAL | `` `Term::kitty_store_image` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | effect-pty-write | parser:verified dispatch:verified effect:verified | verified-with-deviation | wezterm `term/src/terminalstate/kitty.rs` | Rejection IS the spec-compliant behavior on platforms lacking the transport. §13.1 pins parser→dispatch→effect via `kitty_transmission_shared_memory_rejected_with_einval_reply` — asserts EINVAL reply AND no placement created. |

## Format

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-TRANSMIT-FORMAT-24` | kitty graphics-protocol.rst §Pixel formats (`f=24`) | `` `APC G a=t,f=24,s=<w>,v=<h>,...; <rgb> ST` `` | 24-bit RGB — 3 bytes per pixel, `s=` width and `v=` height mandatory | `` `Term::kitty_decode_pixels` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Missing `s=`/`v=` → EINVAL reply; non-multiple-of-3 payload → EINVAL reply. §13.1 pins success path via `kitty_format_24_rgb_stores_and_places`. |
| `KG-TRANSMIT-FORMAT-32` | kitty graphics-protocol.rst §Pixel formats (`f=32`) | `` `APC G a=t,f=32,s=<w>,v=<h>,...; <rgba> ST` `` | 32-bit RGBA — 4 bytes per pixel (default) | `` `Term::kitty_decode_pixels` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Size mismatch vs `w*h*4` → EINVAL reply. §13.1 pins success path via `kitty_format_32_rgba_stores_and_places` + size-mismatch via `kitty_format_32_wrong_payload_size_emits_einval_reply`. |
| `KG-TRANSMIT-FORMAT-100` | kitty graphics-protocol.rst §Pixel formats (`f=100`) | `` `APC G a=t,f=100,...; <png> ST` `` | PNG — decoded via `decode_to_rgba`, `s=`/`v=` optional | `` `Term::kitty_decode_pixels` → `decode_to_rgba` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | texture-render | parser:verified dispatch:verified snapshot:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | PNG decode failure → EINVAL reply. §13.1 pins success path via `kitty_format_100_png_stores_and_places`. |
| `KG-FORMAT-UNSUPPORTED` | kitty graphics-protocol.rst §Pixel formats (validation) | `` `APC G a=t,f=<other>,...; <data> ST` `` | Unsupported-format — emits `EINVAL: unsupported format` PTY reply | `` `Term::kitty_decode_pixels` (`oriterm_core/src/term/handler/image/kitty/store.rs`) `` | effect-pty-write | parser:verified dispatch:verified effect:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Pin is the PTY-reply emission, NOT the unused `KittyError::UnsupportedFormat` enum variant in `parse.rs`. §13.1 pins via `kitty_format_unsupported_emits_einval_reply`. |

## Compression

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-COMPRESSION-OZ-IGNORED` | kitty graphics-protocol.rst §Compression (`o=z`) | `` `APC G a=t,o=z,...; <zlib-data> ST` `` | Zlib compression flag — parsed into `KittyCommand::compression` but NOT consumed downstream | `` `apply_key_value` (`oriterm_core/src/image/kitty/parse.rs`) `` | parser-only | parser:pending | verified-with-deviation | wezterm `term/src/terminalstate/kitty.rs` | Clients sending `o=z` get garbled payload OR ENOMEM; pin prevents silent no-op from surviving into §13.1/§13.2 aggregates. §13.1 optionally implements decompression; until then this row stays at `verified-with-deviation`. |

## Delete Specifiers

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-DELETE-a` | kitty graphics-protocol.rst §Deleting images (`d=a`) | `` `APC G a=d,d=a ST` `` | Delete all placements visible on screen (keep image data) | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | **BUG-08-7**: current impl calls `cache.clear()` which also drops image data — wrong for lowercase `a`. §13.0.5 fix in `kitty/delete/mod.rs`. |
| `KG-DELETE-A` | kitty graphics-protocol.rst §Deleting images (`d=A`) | `` `APC G a=d,d=A ST` `` | Delete all placements visible on screen + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | `cache.clear()` is correct for uppercase `A`. §13.0.5 separates the two arms. |
| `KG-DELETE-i` | kitty graphics-protocol.rst §Deleting images (`d=i`) | `` `APC G a=d,d=i,i=<id>[,p=<pid>] ST` `` | Delete placements for image id (optionally a specific placement id) | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.0.5 wires the `p=` sub-case (placement-id scoping). |
| `KG-DELETE-I` | kitty graphics-protocol.rst §Deleting images (`d=I`) | `` `APC G a=d,d=I,i=<id>[,p=<pid>] ST` `` | Delete image (and placements) by image id | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-p` | kitty graphics-protocol.rst §Deleting images (`d=p`) | `` `APC G a=d,d=p,x=<col>,y=<row> ST` `` | Delete placements that intersect cell (x,y); keep image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | **BUG-08-7**: current impl uses `placement_id` + `image_id` instead of `x`/`y` cell coordinates — wrong. §13.0.5 fix. |
| `KG-DELETE-P` | kitty graphics-protocol.rst §Deleting images (`d=P`) | `` `APC G a=d,d=P,x=<col>,y=<row> ST` `` | Delete placements that intersect cell (x,y) + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Uppercase companion of `d=p`; §13.0.5 fix. |
| `KG-DELETE-c` | kitty graphics-protocol.rst §Deleting images (`d=c`) | `` `APC G a=d,d=c ST` `` | Delete placements intersecting current cursor position | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | **BUG-08-7**: current impl deletes by column only, ignores row. §13.0.5 fix to use `(cursor_col, cursor_row)`. |
| `KG-DELETE-C` | kitty graphics-protocol.rst §Deleting images (`d=C`) | `` `APC G a=d,d=C ST` `` | Delete placements intersecting current cursor position + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Uppercase companion of `d=c`; shares the row-ignored bug. §13.0.5 fix. |
| `KG-DELETE-x` | kitty graphics-protocol.rst §Deleting images (`d=x`) | `` `APC G a=d,d=x,x=<col> ST` `` | Delete placements intersecting a specific column | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-X` | kitty graphics-protocol.rst §Deleting images (`d=X`) | `` `APC G a=d,d=X,x=<col> ST` `` | Delete placements intersecting a specific column + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-y` | kitty graphics-protocol.rst §Deleting images (`d=y`) | `` `APC G a=d,d=y,y=<row> ST` `` | Delete placements intersecting a specific row | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.0.5 verifies 1-based-viewport → stable-row mapping. |
| `KG-DELETE-Y` | kitty graphics-protocol.rst §Deleting images (`d=Y`) | `` `APC G a=d,d=Y,y=<row> ST` `` | Delete placements intersecting a specific row + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-z` | kitty graphics-protocol.rst §Deleting images (`d=z`) | `` `APC G a=d,d=z,z=<i32> ST` `` | Delete placements with the specified z-index | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-Z` | kitty graphics-protocol.rst §Deleting images (`d=Z`) | `` `APC G a=d,d=Z,z=<i32> ST` `` | Delete placements with the specified z-index + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | §13.1 drives to verified. |
| `KG-DELETE-r` | kitty graphics-protocol.rst §Deleting images (`d=r`) | `` `APC G a=d,d=r,x=<lo>,y=<hi> ST` `` | Delete images whose id ∈ [x, y] (kitty 0.33.0+ id-range delete) | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | **BUG-08-7**: current impl deletes by cursor position, not id-range. §13.0.5 rewrites against `ImageCache::remove_image_range`. |
| `KG-DELETE-R` | kitty graphics-protocol.rst §Deleting images (`d=R`) | `` `APC G a=d,d=R,x=<lo>,y=<hi> ST` `` | Delete images whose id ∈ [x, y] + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Uppercase companion of `d=r`; §13.0.5 fix. |
| `KG-DELETE-n` | kitty graphics-protocol.rst §Deleting images (`d=n`) | `` `APC G a=d,d=n,I=<num>[,p=<pid>] ST` `` | Delete placements for the newest image with `I=` number | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Current impl logs-and-skips; §13.0.5 implements `ImageCache::resolve_newest_by_number`. |
| `KG-DELETE-N` | kitty graphics-protocol.rst §Deleting images (`d=N`) | `` `APC G a=d,d=N,I=<num>[,p=<pid>] ST` `` | Delete newest image by number + free image data | `` `Term::kitty_delete` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Current impl logs-and-skips; §13.0.5 fix. |
| `KG-DELETE-q` | kitty graphics-protocol.rst §Deleting images (`d=q`) | `` `APC G a=d,d=q,x=<col>,y=<row>,z=<i32> ST` `` | Delete placements intersecting (x,y) at a specific z-index | `` `Term::kitty_delete_at_cell_with_z` (`oriterm_core/src/term/handler/image/kitty/delete/mod.rs`) + `ImageCache::remove_placements_at_cell_with_z` `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Added by §13.0.5. |
| `KG-DELETE-Q` | kitty graphics-protocol.rst §Deleting images (`d=Q`) | `` `APC G a=d,d=Q,x=<col>,y=<row>,z=<i32> ST` `` | Delete placements intersecting (x,y) at z-index + free image data | `` `Term::kitty_delete_at_cell_with_z` (`oriterm_core/src/term/handler/image/kitty/delete/mod.rs`) + `ImageCache::prune_if_orphaned` `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Added by §13.0.5. |
| `KG-DELETE-f` | kitty graphics-protocol.rst §Deleting images (`d=f`) | `` `APC G a=d,d=f,i=<id>[,r=<frame>] ST` `` | Delete animation frames for an image | `` `Term::kitty_delete_frame` (`oriterm_core/src/term/handler/image/kitty/delete/mod.rs`) + `ImageCache::{has_extra_animation_frames,remove_animation_frame}` `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Static-image case is a no-op per kitty graphics.c:1696; animated-image frame removal adjusts `animation_state.current_frame`. Added by §13.0.5. |
| `KG-DELETE-F` | kitty graphics-protocol.rst §Deleting images (`d=F`) | `` `APC G a=d,d=F,i=<id>[,r=<frame>] ST` `` | Delete animation frames + free image data if fully drained | `` `Term::kitty_delete_frame` (`oriterm_core/src/term/handler/image/kitty/delete/mod.rs`) + `ImageCache::remove_image` on drain `` | state-snapshot | parser:verified dispatch:verified state:verified | verified | wezterm `term/src/terminalstate/kitty.rs` | Uppercase companion of `d=f`; static-image case removes the image entirely per kitty graphics.c:1696. Added by §13.0.5. |

## Frame Actions (`a=f`)

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-FRAME-TRANSMIT` | kitty graphics-protocol.rst §Animation (frame loading) | `` `APC G a=f,i=<id>,...; <data> ST` `` | Append a new animation frame to an existing image | `` `Term::kitty_frame` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Default when neither `c=` nor `r=` is set. §13.3 drives to verified. |
| `KG-FRAME-REPLACE` | kitty graphics-protocol.rst §Animation (frame loading, `c=`) | `` `APC G a=f,i=<id>,c=<frame>,...; <data> ST` `` | Create/replace the frame whose image data serves as the base for frame N | `` `Term::kitty_frame` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |
| `KG-FRAME-EDIT` | kitty graphics-protocol.rst §Animation (frame loading, `r=`) | `` `APC G a=f,i=<id>,r=<frame>,...; <data> ST` `` | Edit the pixel data of frame N in place | `` `Term::kitty_frame` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |
| `KG-FRAME-COMPOSITE-OVERWRITE` | kitty graphics-protocol.rst §Animation (frame loading, `X=1`) | `` `APC G a=f,i=<id>,X=1,...; <data> ST` `` | Overwrite composition mode — destination pixels replace source pixels | `` `kitty_frame` — `cell_x_offset == 1` branch (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Note: `X=` key is overloaded — for `a=f` it means composition mode (0/1), NOT the placement x-offset. §13.3 drives to verified. |
| `KG-FRAME-COMPOSITE-ALPHABLEND` | kitty graphics-protocol.rst §Animation (frame loading, default) | `` `APC G a=f,i=<id>,...; <data> ST` `` | Alpha-blend composition mode — default when `X=` absent or `X=0` | `` `kitty_frame` — default branch (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |

## Animate Actions (`a=a`)

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-ANIMATE-STOP` | kitty graphics-protocol.rst §Animation control (`s=1`) | `` `APC G a=a,i=<id>,s=1 ST` `` | Stop animation playback | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | `s=` is overloaded — for `a=a` it means action (1/2/3), NOT source width. §13.3 drives to verified. |
| `KG-ANIMATE-RUN-WAIT` | kitty graphics-protocol.rst §Animation control (`s=2`) | `` `APC G a=a,i=<id>,s=2 ST` `` | Run animation and wait for new frames when exhausted | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |
| `KG-ANIMATE-RUN` | kitty graphics-protocol.rst §Animation control (`s=3`) | `` `APC G a=a,i=<id>,s=3 ST` `` | Run animation (free-running loop) | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |
| `KG-ANIMATE-LOOP-COUNT` | kitty graphics-protocol.rst §Animation control (`v=`) | `` `APC G a=a,i=<id>,v=<n> ST` `` | Set number of playback loops (0 = infinite) | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | `v=` overloaded — for `a=a` it means loop count, NOT source height. §13.3 drives to verified. |
| `KG-ANIMATE-SET-CURRENT-FRAME` | kitty graphics-protocol.rst §Animation control (`r=`, `c=`) | `` `APC G a=a,i=<id>,r=<frame> ST` `` or `` `APC G a=a,i=<id>,c=<frame> ST` `` | Set the current/displayed animation frame (1-based) | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Current impl calls `set_current_frame` for both `r=` and `c=`; §13.3 verifies per-key semantics. |
| `KG-ANIMATE-SET-FRAME-GAP` | kitty graphics-protocol.rst §Animation control (`z=`) | `` `APC G a=a,i=<id>,z=<ms> ST` `` | Set gap-to-next-frame (milliseconds) for the current frame | `` `Term::kitty_animate` (`oriterm_core/src/term/handler/image/kitty_animation.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.3 drives to verified. |

## Response Codes

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-RESPONSE-OK` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;OK ST` `` | Success reply emitted after every accepted action unless quiet≥1 | `` `Term::kitty_respond` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | §13.5 drives to verified. |
| `KG-RESPONSE-EBADF` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;EBADF:<msg> ST` `` | Bad file descriptor / file not openable | MISSING — to be added by §13.5 | effect-pty-write | parser:missing dispatch:missing effect:missing | missing | wezterm `term/src/terminalstate/kitty.rs` | Current impl lumps file-open failures into EIO; §13.5 splits EBADF (descriptor/open) vs EIO (runtime read). |
| `KG-RESPONSE-EBIG` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;EBIG:<msg> ST` `` | Payload exceeds the per-image max-byte budget | MISSING — to be added by §13.5 | effect-pty-write | parser:missing dispatch:missing effect:missing | missing | wezterm `term/src/terminalstate/kitty.rs` | Current impl returns ENOMEM for oversize; §13.5 distinguishes EBIG (static cap) vs ENOMEM (runtime alloc fail). |
| `KG-RESPONSE-EINVAL` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;EINVAL:<msg> ST` `` | Invalid argument — missing s=/v=, bad format, bad shape | `` `Term::kitty_decode_pixels` + `Term::kitty_store_image` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | Emitted from multiple validation sites; §13.5 pins emission points. |
| `KG-RESPONSE-ENOENT` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;ENOENT:<msg> ST` `` | Image id not found (place / animate on unknown id) | `` `Term::kitty_place` + `Term::kitty_frame` (`oriterm_core/src/term/handler/image/kitty.rs` + `kitty_animation.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | §13.5 drives to verified. |
| `KG-RESPONSE-ENOMEM` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;ENOMEM:<msg> ST` `` | Allocation / cache insert failure | `` `Term::kitty_store_image` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | §13.5 drives to verified. |
| `KG-RESPONSE-EIO` | kitty graphics-protocol.rst §Error handling | `` `APC G i=<id>;EIO:<msg> ST` `` | File read / IO failure during transmission | `` `Term::kitty_store_from_file` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | §13.5 drives to verified. |
| `KG-RESPONSE-QUIET` | kitty graphics-protocol.rst §Suppressing responses (`q=`) | `` `APC G ...,q=<0|1|2>,...; ST` `` | Quiet-level gating — q=0 emit all, q=1 suppress OK only, q=2 suppress all | `` `Term::kitty_respond` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | effect-pty-write | parser:pending dispatch:pending effect:pending | implemented-unverified | wezterm `term/src/terminalstate/kitty.rs` | Behavioral row; §13.5 pins q=0/q=1/q=2 gating with matrix tests per error code. |

## Unicode Placeholders (U=1)

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-UNICODE-PLACEHOLDER-TRANSMIT-U1` | kitty graphics-protocol.rst §Unicode placeholders | `` `APC G a=T,U=1,...; <data> ST` `` | Transmit-and-place with U=1 — placement deferred to placeholder cells | `` `Term::kitty_transmit_and_place` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | Image stored but `kitty_create_placement` NOT called. §13.4 drives to verified. |
| `KG-UNICODE-PLACEHOLDER-PLACE-U1` | kitty graphics-protocol.rst §Unicode placeholders | `` `APC G a=p,U=1,i=<id>,... ST` `` | Place with U=1 — placement deferred to placeholder cells | `` `Term::kitty_place` (`oriterm_core/src/term/handler/image/kitty.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending | stub | wezterm `term/src/terminalstate/kitty.rs` | §13.4 drives to verified. |
| `KG-UNICODE-PLACEHOLDER-CELL-RESOLVE` | kitty graphics-protocol.rst §Unicode placeholders (cell resolution) | U+10EEEE + diacritic row/column encoding → image lookup at render time | Cell-level placeholder rendering — resolve `U+10EEEE` + row/column diacritics to an `ImageId` and rasterize at paint | MISSING — to be added by §13.4 (placeholder resolver in `oriterm_core::image::placeholder`) | texture-render | parser:missing dispatch:missing snapshot:missing | missing | wezterm `term/src/terminalstate/kitty.rs` | Section 13.4 writes the resolver + paint-path integration. |
| `KG-UNICODE-PLACEHOLDER-REFLOW` | kitty graphics-protocol.rst §Unicode placeholders (reflow) | Placeholder cells move with text under reflow / scroll / insert / delete | Placeholder cells track their host row under reflow, scrollback eviction, and scroll-region operations | MISSING — to be added by §13.4 | state-snapshot | parser:missing dispatch:missing state:missing | missing | wezterm `term/src/terminalstate/kitty.rs` | §13.4 drives to verified; pairs with Section 11 (Unicode subcell glyphs) for reflow correctness. |

## Cross-Stack (Kitty × Sixel)

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| `KG-CROSS-STACK-SIXEL-MIXED-Z-ORDER` | kitty graphics-protocol.rst §Z-index + sixel layering (cross-stack) | Kitty placement with z-index mixed with sixel raster in the same viewport | Z-order composition when kitty and sixel images share the same cell region | MISSING — to be added by §13.6 | texture-render | parser:missing dispatch:missing snapshot:missing | missing | wezterm `term/src/terminalstate/kitty.rs` + sixel backend | §13.6 drives to verified. |
| `KG-CROSS-STACK-SIXEL-MIXED-EVICTION` | kitty graphics-protocol.rst §Memory budget + sixel lifecycle (cross-stack) | Eviction coherence when both stacks share the image cache's per-image max-byte budget | Eviction semantics when both stacks compete for the same cache budget | MISSING — to be added by §13.6 | state-snapshot | parser:missing dispatch:missing state:missing | missing | wezterm `term/src/terminalstate/kitty.rs` + sixel backend | §13.6 drives to verified. |
| `KG-CROSS-STACK-SIXEL-PLACEHOLDER-COEXIST` | kitty graphics-protocol.rst §Unicode placeholders (cross-stack) | U=1 placeholder cells coexisting with sixel raster on the same row | Placeholder cells vs sixel raster on the same row (reflow + paint) | MISSING — to be added by §13.6 | texture-render | parser:missing dispatch:missing snapshot:missing | missing | wezterm `term/src/terminalstate/kitty.rs` + sixel backend | §13.6 drives to verified; depends on §13.4 landing first. |
