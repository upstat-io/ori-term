---
schema_version: "1.0"
stack: iterm2
title: "iTerm2 Proprietary Sequences Catalog"
owner_section: "01 (bootstrap), 10 (non-image), 14 (image)"
---

# iTerm2 Proprietary Sequences Catalog

iTerm2 introduced several proprietary sequences via OSC 1337 and other numeric OSCs. Section 10 (OSC Suite) owns the non-image OSC 1337 sub-ops (`SetMark`, `RemoteHost`, `CurrentDir`, `Copy`, `ReportCellSize`, `SetUserVar`, `ShellIntegrationVersion`) and their verification; Section 14 (iTerm2 Inline Images) owns `File=` and the image-transmit chain.

| ID | Spec source | Sequence | Description | Implementation | Apex layer | Test chain | Verification | De-facto ref | Notes |
|---|---|---|---|---|---|---|---|---|---|
| ITERM2-1337-FILE | iTerm2 proprietary (File=) | `` `OSC 1337 ; File=<key=value,...>:<base64> BEL\|ST` `` | Inline image transmit (File= form) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_file` → `Term::handle_iterm2_file` (`oriterm_core/src/term/handler/image/iterm2.rs`) `` | texture-render | parser:pending dispatch:pending snapshot:pending frame-input:pending texture:pending | implemented-unverified | — | Owner: Section 14 (image). The `File=` path routes through the shared `b"1337"` sub-dispatcher that Section 10.0 refactored; non-image sub-ops are siblings in this catalog and owned by Section 10. |
| ITERM2-1337-REMOTEHOST | iTerm2 proprietary (RemoteHost=) | `` `OSC 1337 ; RemoteHost=<user@host> BEL\|ST` `` | Report current remote host (shell integration) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_remote_host` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | state-snapshot | parser:pass dispatch:pass state:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0 via the `b"1337"` sub-dispatcher refactor. Section 10.7 adds the `Term` override (new field `remote_host: Option<String>`) and verification. Owner: Section 10 (non-image). |
| ITERM2-1337-CURRENTDIR | iTerm2 proprietary (CurrentDir=) | `` `OSC 1337 ; CurrentDir=<path> BEL\|ST` `` | Report current working directory (iTerm2 flavor of OSC 7) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_current_dir` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | state-snapshot | parser:pass dispatch:pass state:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 routes through `Term::set_cwd` (SSOT with OSC 7 and OSC 133). Owner: Section 10 (non-image). |
| ITERM2-1337-COPY | iTerm2 proprietary (Copy=) | `` `OSC 1337 ; Copy=<b64> BEL\|ST` `` | Copy data to clipboard (iTerm2 flavor of OSC 52) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_copy` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | effect-clipboard | parser:pass dispatch:pass effect:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 emits `HostEffect::ClipboardStore` after base64 decode (mirrors OSC 52 store). Owner: Section 10 (non-image). |
| ITERM2-1337-SETMARK | iTerm2 proprietary (SetMark) | `` `OSC 1337 ; SetMark BEL\|ST` `` | Set a mark at the current cursor position (for Cmd+Shift+Up/Down navigation) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_set_mark` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | state-snapshot | parser:pass dispatch:pass state:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 pushes a new entry onto `prompt_markers` (SSOT with OSC 133;A). Owner: Section 10 (non-image). |
| ITERM2-1337-REPORTCELLSIZE | iTerm2 proprietary (ReportCellSize) | `` `OSC 1337 ; ReportCellSize BEL\|ST` `` | Report cell dimensions in pixels — response: `OSC 1337 ; ReportCellSize=H;W ST` | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_report_cell_size` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | effect-pty-write | parser:pass dispatch:pass effect:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 emits `PtyEffect::Write` with the cell-size reply, sourcing H/W from `Term::cell_pixel_height/width`. Owner: Section 10 (non-image). Used by image-display tools (imgcat, viu). |
| ITERM2-1337-SETUSERVAR | iTerm2 proprietary (SetUserVar=) | `` `OSC 1337 ; SetUserVar=<name>=<b64> BEL\|ST` `` | Set a named user variable (shell integration context) | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_set_user_var` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | state-snapshot | parser:pass dispatch:pass state:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 stores into `Term::user_vars: IndexMap<String, String>` with a 256-entry FIFO eviction cap (RSS bound). Owner: Section 10 (non-image). |
| ITERM2-1337-SHELLINTVERSION | iTerm2 proprietary (ShellIntegrationVersion=) | `` `OSC 1337 ; ShellIntegrationVersion=<version> BEL\|ST` `` | Report shell integration version string | `` `osc::dispatch` (`crates/vte/src/ansi/dispatch/osc.rs`) — `b"1337"` sub-dispatcher → `Handler::iterm2_shell_integration_version` (trait default no-op at `crates/vte/src/ansi/handler.rs`) — `Term` override pending Section 10.7 `` | state-snapshot | parser:pass dispatch:pass state:pending | implemented-unverified | — | Dispatch arm landed in Section 10.0. Section 10.7 stores into new field `Term::shell_integration_version: Option<String>`. Owner: Section 10 (non-image). Added as a new catalog row by Section 10. |
